# ADR-014: UUIDv7 globally unique IDs

## Decision

Cortex uses UUIDv7 identifiers for globally unique, time-sortable entities.

## Rationale

They keep later sync and device metadata options open without identifier migration.
