# ADR-007: Local inference through a provider abstraction

## Decision

Inference and embeddings are ports with adapters for a configured local server.

## Rationale

Provider-neutral domain types avoid coupling state to a model vendor.
