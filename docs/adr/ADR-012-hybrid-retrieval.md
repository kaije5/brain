# ADR-012: Hybrid retrieval rather than vector-only search

## Decision

Cortex fuses lexical and embedding candidates with deterministic ranking.

## Rationale

Hybrid retrieval is more resilient than relying only on vector similarity.
