# ADR-008: Nemotron as default profile, not architecture dependency

## Decision

Nemotron is the default local model profile, not a domain dependency.

## Rationale

The model can change without migrating Cortex state or capability contracts.
